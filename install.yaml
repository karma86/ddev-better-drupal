name: better-drupal

pre_install_actions:

project_files:
 - docker-compose.elasticsearch.yaml
 - docker-compose.kibana.yaml

global_files:

post_install_actions:
 - 'echo "composer_root: app" >> .ddev/config.yaml'
 - 'echo "disable_settings_management: true" >> .ddev/config.yaml'